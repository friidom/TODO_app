# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.env

# Database dumps — contain user data, never commit
backups

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# CodeGraph / graphify tool output — regenerated locally, never committed
graphify-out

coverage
Thumbs.db
*.pid
.playwright-mcp
test-results
playwright-report

# Personal tooling, not part of the project. .claude/CLAUDE.md, the agent
# definitions and the comment-guard hook stay tracked; these three do not,
# because they make a clone depend on a `codegraph` binary that nothing here
# installs — the UserPromptSubmit hook then fails on every prompt.
.mcp.json
.claude/settings.json
.claude/settings.local.json

# Personal learning material written against the pre-B5 Supabase design.
# Kept locally, out of the shared repository.
docs/code_details
